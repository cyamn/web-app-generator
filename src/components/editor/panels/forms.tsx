import React from "react";

import { type EditorProperties } from "../shared";

export const Forms: React.FC<EditorProperties> = ({ page }) => {
  return (
    <div className="h-full overflow-scroll bg-white p-4 font-sans leading-normal tracking-normal"></div>
  );
};
